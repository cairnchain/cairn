[package]
name = "cairn-docs"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[[bin]]
name = "cairn-docs"
path = "src/main.rs"

# Nothing that ships depends on this crate, so what it pulls in is not what a
# reader who downloads a program gets. The parser only: the HTML writer that
# comes with it writes somebody else's shape, and this crate writes ours.
[dependencies]
pulldown-cmark.workspace = true
