[package]
name = "cairn-http"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]

[dev-dependencies]
# The request reader is the most exposed parser in this workspace and had no
# fuzz target. `tests/fuzz_request.rs` is that target; nothing a node ships
# gains anything, because this crate's own `[dependencies]` is still empty.
cairn-fuzz.workspace = true
