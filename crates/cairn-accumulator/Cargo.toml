[package]
name = "cairn-accumulator"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
cairn-primitives.workspace = true
thiserror.workspace = true

[dev-dependencies]
cairn-fuzz.workspace = true
# For `tests/archivist_cost.rs`, which counts what serving a proof hashes
# rather than timing it. Only the test build asks for it.
cairn-primitives = { workspace = true, features = ["count-hashing"] }
hex.workspace = true
